In sales>configuration added a new menu "Product Tag".
when we on the debug mode then the "Product Tag" menu is visible.
In this "product tag" it shows the custom tree view which is refered in custom window action but its shows base form view because we have refered the tree view in custom window action.

added a field 'tag_ids' in 'product.template' after name field and also added a same field in kanban view.
created a new group "Manage Properties of Product".
