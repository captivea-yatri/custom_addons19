<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <!-- Tree View -->
        <record id="view_employee_list" model="ir.ui.view">
            <field name="name">employee.list</field>
            <field name="model">employee</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                    <field name="department_name"/>
                    <field name="job_position"/>
                    <field name="salary"/>
                    <field name="hire_date"/>
                    <field name="gender"/>
                    <field name="job_type"/>
                </list>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_employee_form" model="ir.ui.view">
            <field name="name">employee.form</field>
            <field name="model">employee</field>
            <field name="arch" type="xml">
                <form>
                        <group>
                            <field name="name"/>
                            <field name="department_name"/>
                            <field name="job_position"/>
                            <field name="salary"/>
                            <field name="hire_date"/>
                            <field name="gender"/>
                            <field name="job_type"/>
                        </group>
                </form>
            </field>
        </record>



        <!-- Action -->
        <record id="action_employee" model="ir.actions.act_window">
            <field name="name">Employees</field>
            <field name="res_model">employee</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menu -->
        <menuitem id="Employee_KSC" name="Employee_KSC"/>
    <menuitem id="Employee_child" name="Employee" parent="Employee_KSC" action="action_employee"/>



</odoo>
